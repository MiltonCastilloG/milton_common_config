"$schema" = 'https://starship.rs/config-schema.json'

format = """[](fg:#4A7A5C)\
$os\
$username\
[](fg:#4A7A5C bg:#6BAF8A)\
$directory\
[](fg:#6BAF8A bg:#7EC8C0)\
$git_branch\
$git_status\
[](fg:#7EC8C0 bg:#06969A)\
$c\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$maven\
$nodejs\
$bun\
$nim\
$rust\
$scala\
[](fg:#06969A bg:#33658A)\
$docker_context\
[ ](fg:#33658A)\
"""

# Disable the blank line at the start of the prompt
# add_newline = false

# You can also replace your username with a neat symbol like   or disable this
# and use the os module below
[username]
show_always = true
style_user = "bold fg:white bg:#4A7A5C"
style_root = "bold fg:white bg:#4A7A5C"
format = '[  ]($style)'
disabled = false

# An alternative to the username module which displays a symbol that
# represents the current operating system
[os]
style = "bold fg:white bg:#4A7A5C"
disabled = true # Disabled by default

[directory]
style = "bold fg:white bg:#6BAF8A"
format = "[ $path ]($style)"
truncation_length = 1
truncation_symbol = ""

# Here is how you can shorten some long paths by text replacement
# similar to mapped_locations in Oh My Posh:
[directory.substitutions]
"Documents" = "󰈙 "
"Downloads" = " "
"Music" = " "
"Pictures" = " "
"games" = " " 
"repositories" = " " 
"castlemill-landing" = "󱪈"
"kaboom-odm" = "󰚑"
"tetris-clone-frp" = "󱎓" 
# Keep in mind that the order matters. For example:
# "Important Documents" = " 󰈙 "
# will not be replaced, because "Documents" was already substituted before.
# So either put "Important Documents" before "Documents" or use the substituted version:
# "Important 󰈙 " = " 󰈙 "

[c]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[cpp]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[docker_context]
symbol = " "
style = "bold fg:white bg:#33658A"
format = '[ $symbol $context ]($style)'

[elixir]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[elm]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[git_branch]
symbol = ""
style = "bold fg:white bg:#7EC8C0"
format = '[ $symbol $branch ]($style)'

[git_status]
style = "bold fg:white bg:#7EC8C0"
format = '[$all_status$ahead_behind ]($style)'

[golang]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[gradle]
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[haskell]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[java]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[julia]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[maven]
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[nodejs]
symbol = ""
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[bun]
symbol = ""
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[nim]
symbol = "󰆥 "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[rust]
symbol = ""
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[scala]
symbol = " "
style = "bold fg:white bg:#06969A"
format = '[ $symbol ($version) ]($style)'

[time]
disabled = true
time_format = "%R" # Hour:Minute (strftime; color comes from [time] format/style)
style = "bold fg:white bg:#33658A"
format = '[ ♥ $time ]($style)'
